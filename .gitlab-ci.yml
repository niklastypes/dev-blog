image: registry.gitlab.com/pages/hugo:latest

pages:
  stage: deploy
  script:
    - git submodule update --init --recursive
    - hugo --minify
  artifacts:
    paths:
      - public
  only:
    - main
