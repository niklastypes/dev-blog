image: registry.gitlab.com/pages/hugo/hugo_extended:latest

pages:
  stage: deploy
  script:
    - hugo mod get -u ./...
    - hugo mod tidy
    - hugo --minify
  artifacts:
    paths:
      - public
  only:
    - main
